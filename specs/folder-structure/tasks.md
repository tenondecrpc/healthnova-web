# Tasks — Folder Structure

## Definition of done

- [x] Directory structure created under `src/`
- [x] Route groups `(auth)` and `(dashboard)` created with layouts
- [x] Existing files (`components/ui/button.tsx`, `lib/utils.ts`) untouched
- [x] `components.json` aliases resolve correctly
- [x] `npm run build` passes
